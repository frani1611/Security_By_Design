apiVersion: v1
kind: Service
metadata:
  name: frontend
  labels:
    app: social-media-dashboard
    tier: frontend
spec:
  selector:
    app: social-media-dashboard
    tier: frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
